#uhmmm idk idk

class InventoryHoldings:
    __init__(self):
        self.stack = ()
        
def wentToDTAT(self):
    self.stack = dtatHolding
    if partHoldTime == #put time here :
        partHoldTime == 'true'
        if partHoldTime != #put time here :
            partHoldTime != 'false'
            
            
            while True partHoldTime == 'True' or 'False':
                dtatHolding.append(nameOfPart)
                elif:
                    dtatHolding.pop(nameOfPart)

def ifDtat():
    inputFileName = input( "Enter O2D excel file name file name: ")
    for line in inputFileName:
        inputFileName = open("inputFileName", "r")
        inputFileName.split(",")
